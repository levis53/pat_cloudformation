pipeline {
    agent any
    stages {
        stage('Submit Stack') {
            steps {
            sh "aws cloudformation deploy --template-file template-s3.json --stack-name tata"
              }
             }
            }
            }
